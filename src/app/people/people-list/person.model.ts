// Adding a typescript feature called interface
// Like a class that defines how an object looks but can be instanciated
// More like a contract

export interface Person {
  id: string;
  name: string;
  role: string;
}
